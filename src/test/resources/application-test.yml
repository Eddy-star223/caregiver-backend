spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  h2:
    console:
      enabled: true

paystack:
  secret:
    key: test-jwt-secret-key-that-is-definitely-32-chars!!
  base:
    url: https://api.paystack.co

jwt:
  secret: test-jwt-secret-key-that-is-definitely-32-chars!!
  expiration: 3600000
